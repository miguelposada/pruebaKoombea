// src/notes/database-notes.config.ts

/* import { Module } from '@nestjs/common';
import { MongooseModule } from '@nestjs/mongoose';

@Module({
  imports: [
    MongooseModule.forRoot('mongodb://127.0.0.1:27017/NotesDB'), // Conecta a la base de datos 'Notes'
  ],
})
export class DatabaseNotesModule {} */
